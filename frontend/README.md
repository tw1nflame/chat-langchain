# Чат-ассистент - Минималистичный интерфейс

Простой и минималистичный интерфейс чата с React и Tailwind CSS.

## 🚀 Быстрый старт

```bash
cd frontend
npm install
npm run dev
```

## 📁 Структура компонентов

```
src/
├── components/
│   ├── Sidebar.jsx       # Боковая панель с историей чатов
│   ├── ChatWindow.jsx    # Основное окно чата
│   ├── MessageList.jsx   # Список сообщений
│   ├── Message.jsx       # Отдельное сообщение
│   └── ChatInput.jsx     # Форма ввода с прикреплением файлов
├── App.jsx               # Главный компонент (управление состоянием)
├── main.jsx              # Точка входа
└── index.css             # Tailwind стили
```

## 🎯 Архитектура данных

### **Структура чата:**
```javascript
{
  id: "uuid-v4",              // Уникальный идентификатор
  title: "Название чата",     // Заголовок чата
  messages: [...],            // Массив сообщений
  createdAt: Date             // Время создания
}
```

### **Структура сообщения (формат как у ChatGPT):**
```javascript
{
  id: "uuid-v4",              // Уникальный идентификатор
  role: "user|assistant",     // Роль отправителя
  content: "Текст сообщения", // Содержимое сообщения
  files: [...],               // Прикрепленные файлы (опционально)
  timestamp: Date             // Время отправки
}
```

## 🏗 Архитектура компонентов

### **App.jsx** - Главный контейнер
- Управляет состоянием всех чатов (объект с UUID ключами)
- Отслеживает активный чат
- Генерирует UUID для новых чатов
- Обновляет заголовки чатов на основе первого сообщения

### **Sidebar.jsx** - Боковая панель (256px)
- Кнопка создания нового чата
- Список чатов, отсортированных по времени создания
- Выделение активного чата синей полосой
- Превью последнего сообщения (40 символов)

### **ChatWindow.jsx** - Центральная область
- Получает объект чата целиком
- Управляет отправкой новых сообщений
- Симулирует ответы ассистента
- Обновляет сообщения через callback

### **MessageList.jsx** - Область сообщений
- Прокручиваемый список сообщений
- Максимальная ширина 768px по центру
- Передает сообщения в Message компонент

### **Message.jsx** - Отдельное сообщение
- Поддержка ролей: `user` и `assistant`
- Аватары: "У" для пользователя, "А" для ассистента
- Разные стили для разных ролей
- Отображение прикрепленных файлов

### **ChatInput.jsx** - Форма ввода
- Многострочное поле ввода
- Кнопка прикрепления файлов (📎)
- Кнопка отправки (➤)
- Превью прикрепленных файлов
- Горячие клавиши (Enter/Shift+Enter)

## 🎨 Дизайн принципы

### Цветовая схема:
- **Sidebar**: `bg-gray-900` (темная)
- **Сообщения пользователя**: `bg-blue-50` с `border-blue-200`
- **Сообщения ИИ**: `bg-white` с `border-gray-200`
- **Активный чат**: выделение синей полосой слева

### Отзывчивость:
- Боковая панель: фиксированная ширина 256px
- Основной контент: `flex-1` (занимает оставшееся место)
- Сообщения: максимальная ширина 768px по центру

## ⚡ Функциональность

### Текущие возможности:
- ✅ Создание новых чатов с уникальными UUID
- ✅ Переключение между чатами в сайдбаре
- ✅ Автоматическое именование чатов по первому сообщению
- ✅ Структура сообщений в формате role/content
- ✅ Отправка текстовых сообщений
- ✅ Прикрепление файлов
- ✅ Симуляция ответов ассистента
- ✅ Сортировка чатов по времени создания
- ✅ Превью последних сообщений в сайдбаре

### Для расширения функциональности:
1. **Подключение к API** - замените симуляцию в `ChatWindow.jsx`
2. **Сохранение в localStorage** - добавьте персистентность чатов
3. **Удаление чатов** - добавьте функцию удаления
4. **Поиск** - поиск по истории сообщений
5. **Экспорт** - сохранение чатов в файл
6. **Настройки** - темы, размер шрифта

## 🛠 Как развивать

### Добавление нового компонента:
1. Создайте файл в `src/components/`
2. Используйте функциональный компонент
3. Экспортируйте по умолчанию
4. Импортируйте в родительский компонент

### Подключение к реальному API:
В `ChatWindow.jsx` замените симуляцию на:
```javascript
const response = await fetch('/api/chat', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    messages: [...chat.messages, userMessage]
  })
})
const data = await response.json()
```

### Добавление персистентности:
```javascript
// В App.jsx добавить в useEffect
useEffect(() => {
  localStorage.setItem('chats', JSON.stringify(chats))
}, [chats])

// При инициализации
const [chats, setChats] = useState(() => {
  const saved = localStorage.getItem('chats')
  return saved ? JSON.parse(saved) : initialChats
})
```

### Стилизация:
- Используйте Tailwind utility классы
- Группируйте связанные стили
- Применяйте hover/focus состояния
- Следуйте существующей цветовой схеме

### Состояние:
- Локальное состояние - `useState` в компоненте
- Глобальное состояние - поднимайте в `App.jsx`
- Для сложной логики - добавьте `useReducer`

Проект готов к разработке и легко расширяется!
